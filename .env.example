# Der Gemini API-Key wird absichtlich NICHT über Vite/.env an den Renderer gegeben.
# Er wird in der App unter Einstellungen -> KI eingegeben und mit Electron safeStorage gespeichert.
# Nur für Entwicklung wird VITE_DEV_SERVER_URL intern vom npm-Script gesetzt.
